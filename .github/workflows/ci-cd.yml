# À compléter au Jour 2.
# Squelette : build, test, versionnement, déploiement pré-prod.

name: CI/CD

on:
  push:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install -r requirements.txt
      # TODO : build de l'image Docker
      # TODO : exécution des tests unitaires et d'intégration
      # TODO : job d'évaluation automatisée (quality gate) — Jour 3
      # TODO : déploiement en environnement de pré-production
