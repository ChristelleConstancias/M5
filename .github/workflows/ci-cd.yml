# À compléter au Jour 3.
# Squelette : build, test, évaluation (quality gate MLflow), déploiement pré-prod.

name: CI/CD

on:
  push:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install -r requirements.txt
      # TODO : build de l'image Docker
      # TODO : exécution des tests unitaires et d'intégration
      # TODO : job d'évaluation automatisée (scripts/evaluate_model.py) — quality gate MLflow
      # TODO : déploiement en environnement de pré-production si le quality gate passe
